# Quick Start Guide

## ✅ Server is Running!

Your website is now live at: **http://localhost:3000**

## Easy Start (For Future)

Instead of typing long commands, just double-click:
- **start-dev.bat** - This will start the development server

## Manual Start (If Needed)

If you need to start manually, use:
```powershell
cd "c:\Users\Administrator\Desktop\Tradeware UI"
C:\nodejs\node-v24.13.0-win-x64\npm.cmd run dev
```

## Stop Server

Press `Ctrl + C` in the terminal to stop the server.

## Important URLs

- **Home**: http://localhost:3000
- **Inventory**: http://localhost:3000/inventory  
- **Admin**: http://localhost:3000/admin
- **Contact**: http://localhost:3000/contact

## Troubleshooting

If port 3000 is busy, the server will use the next available port (3001, 3002, etc.)
Check the terminal output to see which port is being used.
